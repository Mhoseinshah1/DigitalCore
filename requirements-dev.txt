# Development / test dependencies (install on top of requirements.txt).
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
anyio==4.7.0
aiosqlite==0.20.0

# Optional static analysis (not required by CI yet).
ruff==0.8.4
mypy==1.13.0
