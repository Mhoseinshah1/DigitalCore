# Backend API
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Auth / crypto
pyjwt==2.10.1
bcrypt==4.2.1
cryptography==44.0.0

# Config / validation
# pydantic is capped below 2.10 by aiogram 3.15.
pydantic==2.9.2
pydantic-settings==2.7.1
email-validator==2.2.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Cache
redis==5.2.1

# Telegram bot
aiogram==3.15.0

# HTTP client — imported at startup by the 3X-UI integration (app/xui/*), which
# the web panel (app/web/views.py) and the bot (admin.xui handler) both load.
# MUST stay a runtime dependency: the backend and bot fail to import without it.
httpx==0.28.1

# --- Retained for the dormant Phase 0 admin-panel templates (rebuilt in Phase 2) ---
jinja2==3.1.6
python-multipart==0.0.20
itsdangerous==2.2.0

# QR codes for V2Ray subscription links (Phase 6). Imported lazily in
# app/services/subscription_service.py; provisioning degrades gracefully to a
# text-only delivery if this (or Pillow) is unavailable at runtime.
qrcode[pil]==8.0
